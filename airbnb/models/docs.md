{% docs dim_listings_cleansed_min_nights %}
Minimum number of the stays requires to rent the hotel.

Even 'minimum_nights' standas for the `1` the value we have `0` in the source tables. The cleaning alogrithm changes to 1 even if the values are 0.

{% enddocs %}